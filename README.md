# sbb-angular-experimental

## Local Development

Incrementally build `angular-experimental` library on changes
```shell
npm run watch
```

Start `angular-experimental-examples` showcase application
```shell
npm run start
```
